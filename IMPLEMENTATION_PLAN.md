# HeadroomRoute 实施计划

更新日期：2026-08-08

当前代码基线：`Cargo.toml 0.8.2`

路线图状态：`实施中`

## 后续路线图

按优先级分 P0 / P1 / P2 三档，每档内按编号顺序执行。

### P0.1 统一只读预检与可操作 doctor 报告

- 状态：`已完成`
- 目标：提供统一的只读预检入口，并把诊断收敛为可直接执行的 doctor 报告。
- 验收标准：
  - 新增 `precheck` 预检入口，`--doctor` 与托盘复用同一套预检逻辑。
  - 报告每一项结论都给出可执行动作，不要求用户组合多条状态。
  - 不执行任何破坏性或写入性操作。
  - 98 项测试通过；`cargo clippy --all-targets -- -D warnings` 与 `cargo fmt -- --check` 通过。
  - `0.8.2` 打包门禁已通过；当前产物未签名，仅用于本地测试。

### P0.2 首次启动预检向导与一键复检

- 状态：`已完成`
- 目标：首次启动引导用户完成预检，之后可随时一键复检。
- 验收标准：
  - 首次启动显示预检向导，逐项给出结论。
  - 失败项提供直接可执行的修复入口，失败不覆盖原有有效配置。
  - 托盘提供一键复检，结果与 `--doctor` 口径一致。
  - 包含最小回归测试并通过完整验证链。
- 验证记录（2026-08-08）：
  - 托盘“设置与诊断”新增“运行启动预检...”，可复制脱敏报告、重新检测或关闭。
  - 首次运行自动打开预检向导；`--headless-seconds` / `--approval-demo` 不触发。
  - 预检在后台线程收集，模态对话框不阻塞托盘；修复动作去重后通过现有托盘命令执行（选择 Headroom Python / 同步 Codex + Claude / CC-Switch / 打开 config.json）。
  - 启动门禁与预检共用 `mode_needs_headroom`，环境不可用时标记 `runtime-unavailable`，不覆盖原有有效配置。
  - 修复动作委托给 owner 托盘窗口（`handle_command((*dialog).parent, …)`），预检窗口保持打开供用户点击“重新检测”。
  - 新增回归测试 8 项（总计 106 项通过）；`cargo clippy --all-targets -- -D warnings` 与 `cargo fmt -- --check` 通过。
  - `0.8.2` 打包门禁已通过；当前产物未签名，仅用于本地测试。
- 验收修正（2026-08-08）：
  - 启动 Python 探测改用短路：`should_probe_python_at_startup` 仅当 `mode_needs_headroom` 为真时才调用 `runtime::find_valid_python`；旁路、已启用协议全部直连或全部禁用时明确不探测。
  - 预检窗口按 owner 所在显示器工作区定位并 DPI 缩放（`precheck_dpi`/`precheck_work_area` 复用 `GetDpiForWindow`/`GetDpiForSystem`、`MonitorFromWindow`/`GetMonitorInfoW`）；纯布局函数 `precheck_layout` 同时产出完整窗口位置尺寸、各控件矩形与字体高度，报告编辑区吸收剩余高度，按钮不重叠、不被裁切，窗口整体落在工作区内。
  - 撤销 `editor_control` 给全部 EDIT 加 `WS_EX_CLIENTEDGE` 的全局改动；仅预检报告编辑框改用专用 `precheck_report_edit` 设置边框，不影响既有故障转移编辑器。
  - 校验关闭时后台 worker 不持有任何窗口指针（仅持有 config 与通道发送端）；顶层创建失败、`WM_NCDESTROY` 的 `PRECHECK_ACTIVE` 复位与对话框 Box 释放仍恰好一次，模态循环异常退出后补 `DestroyWindow` 兜底。
  - 新增回归测试 8 项（启动短路 1 + 预检布局 7），主程序 97 + CLI 17 = 总计 114 项通过；`cargo fmt -- --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test` 全部通过。`0.8.2` 打包门禁已通过；当前产物未签名，仅用于本地测试。
- 最终内部验收（2026-08-08）：
  - 极小工作区改用紧凑布局，完整窗口（含标题栏与边框）不再超过当前显示器工作区；动作按钮使用紧凑标签并补充窗口边界、控件裁切和重叠回归测试。
  - 首次自动打开时显式把预检窗口激活到前台，避免窗口已创建但被终端或其他前台窗口遮挡；窗口随后取消置顶，不长期覆盖其他应用。
  - Windows 实机截图确认窗口为 796 x 639，完整落在 2560 x 1392 工作区内，9 个子控件无重叠或裁切，报告完成态显示 7 项通过。
  - 实机交互确认“重新检测”会进入检测中并禁用按钮，完成后恢复；重复打开仍只有一个窗口；关闭后托盘和进程继续运行，并可再次打开。
  - 临时 GUI 验收入口已撤销，正式逻辑仍只在首次启动自动打开；`cargo fmt -- --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test`（115 项）与 `git diff --check` 全部通过。

### P0.3 安装升级回滚、代码签名与兼容性基线

- 状态：`已完成`
- 目标：让安装与升级可回滚、可签名，并明确支持范围。
- 验收标准：
  - 安装器在失败时恢复原状态，升级前保留可回滚备份。
  - 发布 EXE 带代码签名，签名验证失败给出明确警告。
  - 记录并验证支持的 Windows、Python 与两套 CLI 版本兼容性基线。
  - 包含最小回归测试并通过完整验证链。

### P0.4 正常/降级/旁路/直连/恢复中的明确状态模型

- 状态：`已完成`
- 目标：统一各运行模式的命名与判定，状态迁移清晰可解释。
- 验收标准：
  - 正常、降级、旁路、直连、恢复中五种模式在托盘、完整状态与诊断报告口径一致。
  - 每种模式有明确的进入与退出条件，迁移原因可读。
  - 状态切换不干扰真实请求，恢复过程可观察。
  - 包含最小回归测试并通过完整验证链。

### P1.1 托盘状态中心

- 状态：`已完成`
- 目标：托盘成为单一状态入口，无需组合多处信息。
- 验收标准：
  - 一屏可见 Codex、Claude、Headroom 可用结论与当前模式。
  - 常用操作直接在托盘完成，不新增窗口体系。
  - 与完整状态、诊断报告数据一致。
  - 包含最小回归测试并通过完整验证链。

### P1.2 操作历史、故障切换解释、冷却与撤销

- 状态：`已完成`
- 目标：关键操作与故障切换可回溯，并能在冷却期内撤销。
- 验收标准：
  - 记录关键操作与故障切换历史，包含时间、原因与结果。
  - 故障切换后进入冷却期，避免反复横跳。
  - 支持确认后撤销切换，撤销同样入历史。
  - 历史不含请求正文、API Key 或未脱敏内容。
  - 包含最小回归测试并通过完整验证链。

### P1.3 配置差异预览、备份管理与诊断包

- 状态：`已完成`
- 目标：配置变更前可预览差异，备份可管理，诊断可一键打包。
- 验收标准：
  - 接管前预览 Codex / Claude 配置差异，确认后再写入。
  - 配置备份可列出、恢复，恢复前有确认。
  - 诊断包一键生成，脱敏且不含 API Key。
  - 包含最小回归测试并通过完整验证链。

### P1.4 休眠唤醒、VPN/代理变化、端口冲突和崩溃恢复

- 状态：`已完成`
- 目标：对常见的环境变化与异常做到自动感知和恢复。
- 验收标准：
  - 休眠唤醒后自动复检并恢复可用状态。
  - VPN / 代理变化触发提醒与配置同步。
  - 端口冲突有明确处置，不静默失败。
  - 崩溃重启后自动恢复路由配置并给出记录。
  - 包含最小回归测试并通过完整验证链。

### P2.1 按模型/延迟/成本/成功率的路由策略与只观察模式

- 状态：`已完成`
- 目标：提供可配置的智能路由，先在只观察模式下验证。
- 验收标准：
  - 支持按模型、延迟、成本、成功率选择候选 Provider。
  - 只观察模式记录路由结论但不改变真实流量。
  - 路由决策可见、可解释、可关闭。
  - 包含最小回归测试并通过完整验证链。

### P2.2 脱敏导入导出和稳定本地状态 API

- 状态：`已完成`
- 目标：配置可脱敏迁移，本地状态有稳定读取接口。
- 验收标准：
  - 导入导出不包含 API Key，格式版本化并向前兼容。
  - 提供稳定的本地状态 API，字段与诊断报告一致。
  - 迁移失败不破坏现有配置。
  - 包含最小回归测试并通过完整验证链。

## 统一执行门禁

- 相互独立且文件边界不重叠的编号允许并行实施；共享核心文件的改动必须串行集成和复核。
- 由 Codex 子任务并行实现，主 Codex 负责边界拆分、交叉审查、集成和测试验收；必要时可把独立修复交给 OpenCode。
- 不再逐项要求用户测试；全部路线图开发、自动化验证和 Windows GUI 验收完成后，只交付一次完整真实场景测试清单。
- 任何打包或发布前必须提议并经用户确认版本号。
- 未经授权不提交、推送或发布。
- 不泄露 API Key。

## 执行规则

- 按依赖关系分批并行实施；同一文件的修改由单一工作流负责，避免并发覆盖。
- 每批完成后更新本文件的状态、完成日期和验证记录，再进入集成验证。
- 状态使用：`待实施`、`实施中`、`已完成`、`阻塞`。
- 不捆绑 Python，不新增完整 GUI、Provider 编辑器、云同步或历史图表。
- 未经用户明确授权，不提交、推送或发布。

## 历史区

以下为已完成的历史事项，仅存档，不再作为后续执行清单。

### 后续维护

- 2026-08-03：CC-Switch 变化提醒改为比较 Codex / Claude Provider 快照；数据库中的无关配置变化不再触发通知。
- 2026-08-03：托盘顶部状态取消灰显，保持不可点击但使用正常文字颜色。
- 2026-08-03：托盘 Headroom 指标补充原始/优化语义，大数使用 K/M/B 缩写；完整状态仍保留精确值。
- 2026-08-06：安装器新增 `hr` 快捷命令，将安装目录幂等加入当前用户 PATH，并覆盖 shim 转发、升级备份和失败回滚。
- 2026-08-06：直连模式按 Provider ID 同步 CC-Switch 的标准配置与凭据；正常退出后交还 CC-Switch 当前 Provider，直连期间不回写外部切换。

### 当前基线

- 版本：`0.5.2`
- 工作树存在尚未验证的功能改动，涉及更新可靠性、首次运行提示、故障切换冷却和 Headroom 指标。
- 已知已执行：`cargo fmt`、`git diff --check`。
- 尚未执行完整编译、测试、Clippy 和发布构建验证。

### 1. 基线验证

- 状态：`已完成`
- 目标：确认现有改动稳定，为后续优化建立可信基线。
- 验收标准：
  - 完整审查当前差异，不覆盖用户已有改动。
  - `cargo fmt -- --check` 通过。
  - `cargo check` 通过。
  - `cargo test` 通过。
  - `cargo clippy --all-targets -- -D warnings` 通过。
  - `Build.ps1` 通过。
- 完成日期：2026-07-31
- 验证记录：差异审查完成；`cargo fmt -- --check`、`cargo check`、25 个测试、Clippy 和 `Build.ps1` 全部通过。

### 2. 一键旁路 Headroom

- 状态：`已完成`
- 目标：Headroom 故障时，Codex 和 Claude 仍可通过 HeadroomRoute 使用当前 Provider。
- 验收标准：
  - 托盘可切换“经过 Headroom”和“旁路 Headroom”。
  - 旁路时 CLI 指向本地 HeadroomRoute Agent，不经过压缩层。
  - 当前模式在托盘、完整状态和诊断报告中清晰可见。
  - 模式在程序重启后保持，恢复时安全同步两套 CLI 配置。
  - 包含最小回归测试并通过完整验证链。
- 完成日期：2026-07-31
- 验证记录：新增持久化旁路开关，CLI 在旁路模式下直连本地 Agent；托盘、完整状态和诊断报告显示模式；26 个测试、Clippy 和 `Build.ps1` 通过。

### 3. CC-Switch 变化提醒

- 状态：`已完成`
- 目标：Provider 配置发生变化后及时提醒用户同步。
- 验收标准：
  - 检测 CC-Switch 数据库变化，不持续重复通知同一次变化。
  - 只提醒和提供同步入口，不自动改变当前 Provider。
  - 数据库不存在、暂时不可读或被占用时不影响现有路由。
  - 包含最小回归测试并通过完整验证链。
- 完成日期：2026-07-31
- 验证记录：后台检测数据库修改时间，同一版本只提醒一次，读取失败不影响路由；27 个测试、Clippy 和 `Build.ps1` 通过。

### 4. Headroom 环境选择

- 状态：`已完成`
- 目标：自定义 Headroom 环境不再需要手动编辑 `config.json`。
- 验收标准：
  - 托盘可选择 `python.exe`。
  - 保存前验证 Python 版本、Headroom 版本和 CLI 模块。
  - 验证失败不覆盖原有有效配置，并给出可执行的修复提示。
  - 不自动安装或升级 Python、Headroom。
  - 包含最小回归测试并通过完整验证链。
- 完成日期：2026-07-31
- 验证记录：托盘新增 Python 文件选择入口，验证 Python/Headroom 后才保存，取消或失败不覆盖配置；28 个测试、Clippy 和 `Build.ps1` 通过。

### 5. 统一可用性结论

- 状态：`已完成`
- 目标：用户无需组合多项状态即可判断 Codex、Claude 是否可用。
- 验收标准：
  - 托盘顶部直接显示 Codex、Claude、Headroom 的可用结论。
  - 显示当前为接管模式或旁路模式。
  - 不增加独立仪表盘或新窗口体系。
  - 包含最小回归测试并通过完整验证链。
- 完成日期：2026-07-31
- 验证记录：托盘、完整状态和诊断报告统一展示两套 CLI 的可用结论及接管/旁路模式；29 个测试、Clippy 和 `Build.ps1` 通过。

### 6. 可执行的故障恢复

- 状态：`已完成`
- 目标：把恢复建议收敛为当前最可能有效的操作。
- 验收标准：
  - 能区分运行时、配置同步、Provider 鉴权、网络和本地服务故障。
  - 针对当前故障提供重新同步、重启 Headroom、切换 Provider 或复制诊断等直接动作。
  - 不隐藏原始脱敏错误信息，不执行未经确认的破坏性操作。
  - 包含最小回归测试并通过完整验证链。
- 完成日期：2026-07-31
- 验证记录：托盘按运行时、本地服务、配置同步、上游和未知故障动态提供一个直接建议操作，复用现有安全命令；30 个测试、Clippy 和 `Build.ps1` 通过。

### 7. 区分健康证据

- 状态：`已完成`
- 目标：避免把端口可达误解为 Provider 已可用。
- 验收标准：
  - 展示“尚未验证”“探测可达”“真实请求验证”“鉴权失败”等用户可理解的状态。
  - 自动故障切换仍只选择满足既有安全条件的候选 Provider。
  - 托盘、完整状态和诊断报告口径一致。
  - 包含最小回归测试并通过完整验证链。
- 完成日期：2026-07-31
- 验证记录：路由菜单、完整状态与诊断摘要区分尚未验证、探测可达、真实请求验证和鉴权失败；故障切换条件保持不变；30 个测试、Clippy 和 `Build.ps1` 通过。

### 8. 配置接管提示

- 状态：`已完成`
- 目标：CLI 配置被外部修改时，不再静默恢复而令用户困惑。
- 验收标准：
  - 检测到 Codex 或 Claude 路由配置漂移时通知用户。
  - 明确说明 HeadroomRoute 正在接管配置及其恢复结果。
  - 对同一次漂移避免重复通知，不泄露配置中的凭据。
  - 包含最小回归测试并通过完整验证链。
- 完成日期：2026-07-31
- 验证记录：同一次 CLI 配置漂移只自动恢复一次，并通知恢复结果；失败不循环覆盖，诊断不包含凭据；31 个测试、Clippy 和 `Build.ps1` 通过。

### 9. 明确统计口径

- 状态：`已完成`
- 目标：让 Token 节省量和失败率可被正确理解。
- 验收标准：
  - 明确显示统计范围和起始点。
  - 提供带确认的统计清零操作。
  - 只有 Headroom 日志存在可靠协议字段时才按 Codex、Claude 拆分。
  - 不保存请求正文、API Key 或未经脱敏的日志内容。
  - 包含最小回归测试并通过完整验证链。
- 完成日期：2026-07-31
- 验证记录：统计范围显示为当前日志累计或持久化起始时间；清零只移动日志偏移量，不删除日志；日志无可靠协议字段，未伪造拆分；32 个测试、Clippy 和 `Build.ps1` 通过。

### 10. 自动更新提醒

- 状态：`已完成`
- 目标：及时提醒新版本，同时保持更新由用户控制。
- 验收标准：
  - 启动后每天最多自动检查一次正式版本。
  - 只发送通知，不自动下载或安装。
  - 可在配置中关闭，网络失败不干扰路由服务。
  - 继续复用现有代理、重试、校验和交互式更新流程。
  - 包含最小回归测试并通过完整验证链。
- 完成日期：2026-07-31
- 验证记录：启动后每天最多后台检查一次正式版本，只通知不下载或安装；托盘可关闭并仍保留手动检查；33 个测试、Clippy 和 `Build.ps1` 通过。

### 11. CLI 确认悬浮窗

- 状态：`已完成`
- 目标：Claude Code CLI 和 Codex CLI 等待确认时，在 Windows 桌面顶部提供原生语义的拒绝并反馈、允许一次和允许此类命令操作。
- 设计约束：ConPTY 保持真实终端交互；活动标签页保留原生确认，后台标签页延迟 3 秒提醒；请求无限等待明确选择；最多跟踪 32 个请求；不保存完整终端输出；不使用全局模拟键盘；队列满和托盘不可用时回退原生 CLI 交互。
- 已完成：独立控制台 `HeadroomRouteCLI.exe` wrapper、命名管道 broker、原生 TopMost/NoActivate 悬浮窗、托盘演示入口、请求队列和超时模型；0.6.9 增加结构化 CLI 提示解析、确认期间输入隔离、CLI 退出撤销、当前显示器定位、操作详情及实时倒计时。后台发现 Headroom 环境缺失时只更新托盘状态，不再弹出与当前 CLI 操作无关的通知。
- 完成日期：2026-08-05
- 验证记录：0.6.9 两个二进制共 67 项测试通过，包含 Claude 工作区信任提示不被误判、Ctrl+C 不终止 wrapper、Codex VT 私有模式复位的回归覆盖；进程级中断测试确认子 CLI 返回 Ctrl+C 状态后 wrapper 能完成 ConPTY 排空并正常退出；`cargo fmt -- --check`、`cargo clippy --all-targets -- -D warnings`、release 编译、命名管道 IPC 测试及主程序/CLI 的安装升级与失败回退测试通过；本机 Claude Code 2.1.218 和 Codex CLI 0.146.0 的前台版本命令通过；`Build.ps1` 已生成版本化 EXE、CLI EXE、ZIP 和校验文件并完成 SHA-256 复核。
